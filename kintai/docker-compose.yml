services:
  db:
    image: postgres:16
    container_name: kintai-postgres
    environment:
      POSTGRES_DB: kintai
      POSTGRES_USER: kintai
      POSTGRES_PASSWORD: kintai
    ports:
      - "5432:5432"
    volumes:
      - kintai_pgdata:/var/lib/postgresql/data

volumes:
  kintai_pgdata:
